{
  "name": "n2-slackbot",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "@types/node": "^12.12.14",
    "jest": "^24.9.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.0",
    "ts-jest": "^24.2.0",
    "ts-node": "^8.5.2",
    "typescript": "^3.7.2"
  },
  "scripts": {
    "test": "jest",
    "clean": "rimraf dist/*",
    "tsc": "tsc -p tsconfig.json",
    "cp": "cp -f ./package.json ./yarn.lock ./dist",
    "prodInstall": "yarn install --cwd ./dist  --production",
    "build": "npm-run-all clean tsc cp prodInstall",
    "deploy": "./deploy.sh"
  },
  "jest": {
    "moduleFileExtensions": [
      "ts",
      "js"
    ],
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "globals": {
      "ts-jest": {
        "tsConfig": "tsconfig.json"
      }
    },
    "testMatch": [
      "**/__tests__/**/*.test.ts"
    ]
  }
}
